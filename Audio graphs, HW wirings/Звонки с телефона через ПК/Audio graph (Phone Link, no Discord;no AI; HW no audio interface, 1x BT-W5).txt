@startuml 
title Audio graph (Phone Link, no Discord;no AI; HW: no audio interface, 1x BT-W5)

/' MIC chain '/
rectangle "USB Microphone" as USBMIC
rectangle "Krisp Microphone" as KRISP_MIC
rectangle "VM Stereo In 1 (Krisp Microphone)" as VM_HW1

/' System / Phone Link '/
rectangle "Voicemeeter Input\n-Windows Default Playback Device" as VM_INPUT
rectangle "Phone Link remote audio" as PL_REMOTE

/' Buses '/
rectangle "Voicemeeter Out B1 (Mic only)\n-Windows Default Recording Device" as VM_B1

/' Headphones + BT adapter '/
rectangle "VM A1 Out -> BT-W5 -> TWS\n(Calls)" as A1_CALLS

/' Other apps '/
rectangle "OBS" as OBS


' Mic chain
USBMIC --> KRISP_MIC
KRISP_MIC --> VM_HW1

' Remote chain
PL_REMOTE --> VM_INPUT

' Voicemeeter routing
VM_HW1 --> VM_B1


VM_INPUT --> OBS

VM_INPUT --> A1_CALLS

' Consumers
VM_B1 --> OBS



note as NOTE_CALLS
TWS (Calls):
do NOT route mic to A1
monitor remote only via
Voicemeeter Input -> A1
end note

A1_CALLS .. NOTE_CALLS

@enduml
